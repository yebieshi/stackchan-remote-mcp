# /etc/mosquitto/conf.d/stackchan.conf
#
# This matches the tested v0.1 architecture, but port 1883 is plaintext.
# Read docs/SECURITY.md before exposing it to the internet.

listener 1883 0.0.0.0
allow_anonymous false
password_file /etc/mosquitto/passwd
persistence true
